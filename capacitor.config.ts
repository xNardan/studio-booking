import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.bravejackrabbityawn',
  appName: 'brave-jackrabbit-yawn',
  webDir: 'dist'
};

export default config;
